<%= button_to "Change Password", edit_user_registration_path, { method: :get } %>
<br>
<% if current_user.standard? %>
  <%= link_to 'Upgrade To Premium', new_charge_path, { method: :get } %>
<% elsif current_user.premium? %>
  <%= link_to 'Downgrade To Freemium', edit_charge_path(current_user.id), data: { confirm: "Are you sure?" } %>
<% end %>
<br>
<%= render partial: 'wikis/wikis', locals: {wikis: current_user.wikis, page_title: "My Wikis"} %>